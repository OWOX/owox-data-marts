
# Database Configuration
# This package is database-agnostic - configure TypeORM directly in your app

# Examples for different databases:

# SQLite (recommended for development)
# DATABASE_URL=sqlite:./data/idp.db

# MySQL
# DATABASE_URL=mysql://user:password@localhost:3306/owox_idp

# PostgreSQL  
# DATABASE_URL=postgres://user:password@localhost:5432/owox_idp

# MongoDB (if using TypeORM MongoDB driver)
# DATABASE_URL=mongodb://localhost:27017/owox_idp

# JWT Configuration
JWT_ISSUER=owox-idp
JWT_AUDIENCE=owox-app
ACCESS_TOKEN_TTL=900      # 15 minutes in seconds
REFRESH_TOKEN_TTL=604800  # 7 days in seconds

# Magic Link Configuration
MAGIC_LINK_BASE_URL=http://localhost:3000
MAGIC_LINK_TTL=3600      # 1 hour in seconds

# Email Configuration (for magic links)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_password
EMAIL_FROM=noreply@owox.com

# Environment
NODE_ENV=development