{
  "name": "@owox/idp-better-auth",
  "version": "0.0.0",
  "type": "module",
  "author": "OWOX",
  "license": "ELv2",
  "description": "Better Auth integration for OWOX IDP Protocol",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "test": "jest",
    "lint": "eslint . --config ./eslint.config.js",
    "lint:fix": "eslint . --fix --config ./eslint.config.js",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,js,json,md}\"",
    "typecheck": "tsc --noEmit",
    "prepack": "npm run build",
    "prepublishOnly": "npm audit && npm run lint && npm run typecheck"
  },
  "keywords": [
    "idp",
    "better-auth",
    "authentication",
    "owox"
  ],
  "dependencies": {
    "better-auth": "^1.3.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.1.2",
    "@owox/idp-protocol": "^0.0.0",
    "express": "^4.18.2",
    "typeorm": "^0.3.24"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist",
    "src"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  }
}
