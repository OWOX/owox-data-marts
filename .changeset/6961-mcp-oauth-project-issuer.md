---
'owox': minor
---

# Support issuer-bound OAuth callbacks for project-specific MCP URLs

OAuth clients that require issuer-bound authorization callbacks (RFC 9207), such as Codex, can now
complete sign-in against project-specific MCP URLs (`https://<project>.mcp.owox.com`).
